# NestJS/Next 둘 다 peer 의존성이 느슨해서 자동 설치를 켜두면 버전이 흔들린다.
auto-install-peers=false
strict-peer-dependencies=false

# Vercel의 파일 트레이싱이 pnpm 심볼릭 링크를 따라가지 못하는 경우가 있어
# 워크스페이스 패키지는 링크가 아닌 실제 복사본으로 두지 않는다(링크 유지).
# 배포에서 @mysoulmate/shared 를 못 찾으면 여기에 node-linker=hoisted 를 추가한다.
link-workspace-packages=true
